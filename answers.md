### 1. what is the difference between a function and a procedure in postgresql?
функція обов'язково повертає знач через оператора `return` і може викликатися всередині запитів select, а процедура нічого не повертає через `return` і викликається окремо через `call`;

### 2. can a trigger be executed manually? why or why not?
ні, тригер неможливо запустити вручну за допомогою якоїсь команди, він автоматичний,прив'язаний до конкретної таблиці + події;

### 3. what are the advantages and disadvantages of storing business logic inside the database?
перевага - висока швидкість виконання, т.я. весь код працює всередині бази, поруч із даними без гоняння трафіку по мережі між сервером і бд. + це гарантує централізовану безпеку, бо логіка відпрацює однаково для будь-якого підключеного сервісу чи додатку. з мінусів: бд складніше масштабувати, + у середовищі бд значно важче дебажити код та підтримувати нормальний контроль версій.